# Bible Party V91 — correctif TypeScript

Base : V90 / audit V91.

Correctifs :
- Affichage de la référence du maître de jeu avec un garde de type compatible avec `Question`.
- Ajout des styles mobiles `brand`, `logo` et `tagline` manquants dans `HomeVisual`.
- Remplacement de `StyleSheet.absoluteFillObject` par `StyleSheet.absoluteFill` pour la version React Native utilisée.

La logique de jeu et les contenus existants sont conservés.
